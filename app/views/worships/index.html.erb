
<body>
    <input id="omikuji_btn" type="button" value="おみくじを引く">
    <script>
        document.getElementById("omikuji_btn").onclick = function() {
               var rnd = Math.floor( Math.random() * 6 );
            var omikuji;
            if (rnd === 0) omikuji = "大吉";
            if (rnd === 1) omikuji = "吉";
            if (rnd === 2) omikuji = "中吉";
            if (rnd === 3) omikuji = "小吉";
            if (rnd === 4) omikuji = "末吉";
            if (rnd === 5) omikuji = "凶";
            alert(omikuji);
            
        }
    </script>
</body>



<%= month_calendar do |date| %>
          <%= date %>
          <% end %>
<div class="contents row">
  <% @worships.each do |worship| %>
    <%= render partial: "worship", locals: { worship: worship } %>
  <% end %>
  
</div>

<%= form_with(url: search_worships_path, local: true, method: :get, class: "search-form") do |form| %>
  <%= form.text_field :keyword, placeholder: "投稿を検索する", class: "search-input" %>
  <%= form.submit "検索", class: "search-btn" %>
  
<% end %>


<!-- タグはHTMLにおける要素のかたまりを示す記述 開始タグと、終わりタグがある。全体を要素という-->
<!-- head要素はウェブサイトの基本情報などを記載するブラウザ上には表示されない -->
<!-- body要素はブラウザ上に表示する情報を記載する -->
<!-- DOCTYPEはドキュメントタイプの略で、この文章がHTML文章であることを宣言する。この要素を記述しないと、レイアウトが崩れ、正しく表示されないことがある -->
<!-- html要素はHTML文章の始まりと終わりを示す要素。HTMLを書く際に必要-->
<!-- meta要素はHTML文章に関する情報を指定する際に使用、charsetはmeta要素の属性-->
<!-- 属性は、要素に情報を付け加える役割を持つ。属性の後には属性ちが続く。-->
<!-- 属性値はどのような属性なのかを示す値、-->
<!--<meta charset = "UTF-8>-->
<!-- charsetが属性、"UFT-8"が属性値-->